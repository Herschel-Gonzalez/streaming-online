// node_modules/vue-firebase/index.js
var Plugin = {};
Plugin.install = function(Vue, options) {
  Vue.prototype.$firebase = test(options);
  Vue.mixin({
    beforeCreate() {
      this.$router.beforeEach((to, from, next) => {
        if (to.matched.some((record) => record.meta.requiresAuth)) {
          this.$firebase.auth().onAuthStateChanged(function(user) {
            if (user) {
              next();
            } else {
              next({
                path: to.meta.redirect
              });
            }
          });
        } else {
          next();
        }
      });
    }
  });
};
function test(opt) {
  let fb = opt.firebase;
  let initFb = fb.initializeApp(opt.config);
  return initFb;
}
var vue_firebase_default = Plugin;
export {
  vue_firebase_default as default
};
//# sourceMappingURL=vue-firebase.js.map
